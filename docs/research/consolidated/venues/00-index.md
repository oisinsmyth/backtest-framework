# Venues — where a book could actually be run

[← consolidated index](../00-INDEX.md)

**Twenty-four lanes on the funded-futures-account question, and it reduces to one line.**

> **You cannot extract more in expectation than the amount they let you lose.**

| | one line |
|---|---|
| **[01 · prop account arithmetic](01-prop-account-arithmetic.md)** | `E[extracted] = b` in closed form; **zero strategy families survive, and the reason is geometric** |
| **[02 · rules and conduct](02-prop-rules-and-conduct.md)** | the **one-direction rule reaches resting orders**, and it kills spread/curve/calendar on a rule |

---

## Why this stream is worth more than its verdict

**It is the only research stream whose central result crossed into the repo** — **D386** — and the
crossing then ran the other way: **the decision record corrected the research folder**, which was
amended in place with the withdrawal visible.

**And it exported a standard.** `C19-2`'s falsifier — *run the stale-price / closing-auction
contamination test first* — **had never been demanded of our own overnight result.** D402 ran it, and
the edge survived. → [intraday and overnight](../signals/09-intraday-and-overnight.md) ·
[agreement A6](../conflicts/03-agreements.md)

## The three things to carry out of it, whatever happens to prop

1. **THE SCISSORS CLOSE ON LONG WINDOWS, NOT ON SMALL EDGES.** Every rejection is a *size* rejection;
   **the binding variable is the hold, not the edge.**
2. **Per-trade Sharpe is `t/√N`** — which makes one threshold comparable across every published claim
   in the review.
3. **The one quantity the account is priced on — MAE inside the holding window — is the one quantity
   the literature does not publish, anywhere.** Four targeted probes. **`P(MAE ≤ $2,000/contract)` is
   ~`$8` of Databento inside the existing signup credit.**

## Book status

**`docs/BOOK_PROP.md` carries NONE.** Under the standing rules the prop book needs hurdle **P (R11),
all six**, on top of R8's separate pre-registered out-of-sample test on a fixture it has never seen.

> **An empty book with stated standards beats a populated one with borrowed ones.**
