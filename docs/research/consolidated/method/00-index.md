# Method — how to measure it without fooling ourselves

[← consolidated index](../00-INDEX.md)

**Method and data lanes have no premise number to die on, so what they return is whatever is actually
true.** Rounds 2 and 3 each deliberately commissioned two non-signal lanes for that reason, **and
every time those lanes outproduced the ones beside them.**

| | one line |
|---|---|
| **[01 · benchmark choice](01-benchmark-choice.md)** | round 1's long-leg conflict was really a **benchmark** conflict — and implementing the fix wrongly is worth **13.6 bp/month and a sign** |
| **[02 · nulls and block length](02-nulls-and-block-length.md)** | `b = 20` is harmlessly too long on returns and **~6× too short on state** — and **two `H6` results contradict `CLAUDE.md` directly** |
| **[03 · replication and multiple testing](03-replication-and-multiple-testing.md)** | **35% or 85%** — and why `Var(t)` is the number that does not need a threshold |
| **[04 · tooling hazards](04-tooling-hazards.md)** | four fabrications, nine wrong-200s, five docs-versus-code — **and the defences all cost nothing** |

---

## The three that touch standing guidance in `CLAUDE.md` ▲

| | |
|---|---|
| **per-name rotation p95 bias** | `CLAUDE.md` says it is irreducible. **`H6` says draws with the identity included are exact for any `w` — "the fix is one character of code."** `[NOT APPLIED]` |
| **block length on persistent conditioners** | this session's calibration used ~26 bars/block where the selector wants ~157. **`[OPEN]`, never checked** |
| **`50/P` as the cost-versus-price channel** | **true of the commission, false of the spread.** The empirical verdict that killed D284 is untouched; **which channel caused it is in doubt** |

**All three are the principal's call.** [vs repo R5–R7](../conflicts/02-versus-repo-measurements.md).

## The one methodological finding that has already crossed into the repo

**[FINDINGS §59](../../../FINDINGS.md)** — the best-of-N floor prices selection and is **blind to
sign-fitting**; sixteen pure-noise signals clear it by **+1.28**. **It arrived in a brief and became
repo truth only after being re-derived here in closed form and 200,000 draws, required to agree.**

> **That is the only route across the line, and it is worth stating as a rule of its own: the brief is
> the tip; the repo is the measurement.**

## What research method cannot do here

Under [R15](../../../RULES.md#r15) **nothing in this tree closes or admits anything**, and by the
principal's ruling of 2026-09-09 **nothing is elevated into `FINDINGS.md`, `RULES.md`, the books or a
decision record without his explicit permission.** A tag saying `[EXTERNAL — NOT MEASURED HERE]` **is
not a substitute for that permission** — that mistake was made once and reverted.
